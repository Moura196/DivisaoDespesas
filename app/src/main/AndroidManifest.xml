<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/page_listagem_lancamentos"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThemeStylesIons" >
        <activity
            android:name=".Lancamento.CadastroLancamentoActivity"
            android:exported="false" />
        <activity
            android:name=".Morador.MoradoresActivity"
            android:exported="false" />
        <activity
            android:name=".Lancamento.LancamentosActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Morador.CadastroMoradorActivity"
            android:exported="false" />
    </application>

</manifest>